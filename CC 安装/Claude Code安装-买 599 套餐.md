https://nf.video/ 找到 Claude Code，购买 599 的套餐

![image-20251107010238080](https://p.ipic.vip/9dt3c0.png)

购买成功后来到控制台，1 安装 2 管理

最重要的是管理 API，先创建 API，复制好

![image-20251107010357590](https://p.ipic.vip/pdowe7.png)

在 vsCode 编辑器中，打开 terminal 命令行，配置问题可以使用该脚本配置，输入
npm install -g galaxycodeswitch
安装然后输入 galaxy

如果有什么问题，输入以下命令，在一个叫做 WARP 的软件中

安装 WARP ，打开 agent 模式，输入让它完成的命令

![image-20251107010119473](https://p.ipic.vip/ilpqw2.png)

> "env": {
> "ANTHROPIC_API_KEY": "sk-ant-sid01--替换为新生成的 API ",
> "ANTHROPIC_BASE_URL": "https://relay.nf.video",
> "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": 1
> }
>
> 帮我检查 Claude Code 的 settings.json，加入 API key，并检查 claude code 是 working 的

继续用 galaxy 命令检查是否配置好，在其中按
