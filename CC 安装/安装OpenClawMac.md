# OpenClaw Mac 安装指南

## 一键安装

打开终端，运行以下命令：

```bash
curl -fsSL https://openclaw.ai/install.sh | bash
```

## 说明

- `curl -fsSL`：静默下载安装脚本（`-f` 失败不输出、`-s` 静默、`-S` 显示错误、`-L` 跟随重定向）
- `| bash`：将下载的脚本通过管道传递给 bash 执行

## 安装后

安装完成后，重新打开终端即可使用 OpenClaw。